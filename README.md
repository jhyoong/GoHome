# gohome

`gohome` is a lightweight, single-binary coding agent and terminal UI written in Go.
It targets custom LLM endpoints (OpenAI-compatible and Anthropic-styled wire formats)
and centers two features: tool-call guardrails with human approval, and built-in
sequential subagents you can focus into and steer. See `docs/plans/` for the design and
implementation plan.
