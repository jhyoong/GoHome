# v0.1.0 Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Rename `agent-chat` to `gohome`, inject version via ldflags, add a Dockerfile, set up a CI workflow, and configure GoReleaser with a release workflow so that pushing a `v*` tag produces multi-platform binaries and a Docker image on GHCR.

**Architecture:** GitHub Actions powers two workflows — `ci.yml` for quality checks on every push/PR, and `release.yml` triggered by `v*` tags that delegates all build/package/publish work to GoReleaser. Docker images (linux/amd64, linux/arm64) are pushed to GHCR using the built-in `GITHUB_TOKEN`.

**Tech Stack:** Go 1.25.6, GitHub Actions, GoReleaser v2, Docker Buildx, GHCR

---

### Task 1: Rename `agent-chat` references in `cmd/agent/main.go`

**Files:**
- Modify: `cmd/agent/main.go`

**Step 1: Apply the four renames**

In `cmd/agent/main.go` make these exact changes:

Line 32 — change default config path:
```go
// old
configPath = flag.String("config", "~/.agent-chat/config.yaml", "Path to config file")
// new
configPath = flag.String("config", "~/.gohome/config.yaml", "Path to config file")
```

Line 42-44 — change version print:
```go
// old
fmt.Println("agent-chat", version)
// new
fmt.Println("gohome", version)
```

Line 69 — change default DB path:
```go
// old
cfg.Storage.Path = filepath.Join(home, ".agent-chat", "data.db")
// new
cfg.Storage.Path = filepath.Join(home, ".gohome", "data.db")
```

Line 148 — change startup log:
```go
// old
log.Printf("agent-chat listening on http://%s", httpSrv.Addr)
// new
log.Printf("GoHome listening on http://%s", httpSrv.Addr)
```

**Step 2: Verify the project still builds and tests pass**

```bash
go build ./cmd/agent && go test ./...
```
Expected: build succeeds, all tests pass, no output for tests.

**Step 3: Commit**

```bash
git add cmd/agent/main.go
git commit -m "chore: rename agent-chat to gohome in main.go"
```

---

### Task 2: Update Makefile

**Files:**
- Modify: `Makefile`

**Step 1: Replace binary name throughout**

Replace the entire file content with:

```makefile
.PHONY: build run clean test

build:
	go build -o gohome ./cmd/agent

run: build
	./gohome

test:
	go test ./...

clean:
	rm -f gohome
```

**Step 2: Verify build still works**

```bash
make build
```
Expected: produces `./gohome` binary (not `./agent-chat`).

```bash
ls -la gohome
```
Expected: file exists.

**Step 3: Clean up and commit**

```bash
make clean
git add Makefile
git commit -m "chore: rename binary output from agent-chat to gohome in Makefile"
```

---

### Task 3: Update README.md

**Files:**
- Modify: `README.md`

**Step 1: Apply all renames**

Replace the entire file with this updated content:

```markdown
# GoHome

A single-binary Go server that runs a local AI chat interface in your browser. It connects to any OpenAI-compatible LLM endpoint, executes tools (shell, file read/write) with your approval, and persists sessions in SQLite.

## Requirements

- Go 1.22+
- A local OpenAI-compatible LLM endpoint (e.g. llama.cpp, Ollama)

## Quick Start

```bash
# Build binary
make build

# Run with defaults (connects to http://localhost:8080/v1)
./gohome

# Open http://localhost:3000 in your browser
```

## Configuration

Create `~/.gohome/config.yaml`:

```yaml
endpoint:
  url: "http://localhost:8080/v1"
  model: "my-model"
  max_tokens: 4096
  temperature: 0.7

server:
  host: "127.0.0.1"
  port: 3000

storage:
  path: "~/.gohome/data.db"

system_prompt: "You are a helpful assistant."

approval:
  default_timeout: 300   # seconds to wait for user approval
  auto_approve_all: false
  whitelist:
    - tool: "file_read"
      allow: "always"    # always | never | ask

mcp_servers:
  - name: "my-server"
    transport: "stdio"   # stdio | sse
    command: "my-mcp-server"
    args: ["--flag"]
```

All fields are optional — defaults are used if the config file is missing.

## CLI Flags

```
--config   Path to config file (default: ~/.gohome/config.yaml)
--port     Override server port
--host     Override server host
--db       Override database path
--verbose  Enable debug logging
--version  Print version and exit
```

## Make Targets

```
make build     Build binary
make test      Run all tests
make run       Build and run
make clean     Remove build artifacts
```

## Architecture

```
cmd/agent/          Binary entry point
embed.go            Embeds web/static into the binary
internal/
  config/           YAML config loader with tilde expansion
  session/          SQLite store (sessions, messages, tool results)
  tools/            Tool interface, registry, shell/file_read/file_write
  approval/         Per-connection approval broker (whitelist, timeout)
  llm/              OpenAI-compatible client (streaming SSE)
  mcp/              MCP client (stdio and SSE transports)
  agent/            Agentic loop — streams LLM, executes tools, loops back
  server/           HTTP REST + WebSocket (4-goroutine model per connection)
web/static/         Vanilla JS frontend (no build step)
```

Each WebSocket connection runs 4 goroutines: reader, writer, pingLoop, dispatcher. The agent loop streams tokens to the browser and pauses for user approval before executing any tool.

## Tests

Run all tests:

```bash
go test ./...
```

| Package | Tests |
|---------|-------|
| `internal/config` | `TestParseConfig` — YAML parsing, tilde expansion, defaults |
| `internal/session` | `TestOpenAndMigrate`, `TestSessionCRUD`, `TestMessageCRUD`, `TestToolResultCRUD` |
| `internal/tools` | `TestRegistry`, `TestToLLMTools`, `TestShellTool`, `TestFileReadTool`, `TestFileWriteTool` |
| `internal/approval` | `TestAutoApproveWhitelist`, `TestAutoDenyWhitelist`, `TestAutoApproveAll`, `TestApprovalTimeout`, `TestApprovalContextCancel`, `TestApprovalUserDecision` |
| `internal/llm` | `TestNonStreamingComplete`, `TestStreamingTokens` |
| `internal/agent` | `TestSimpleMessageRoundtrip` — full loop with mock LLM, verifies streaming and persistence |
| `internal/server` | `TestListSessions`, `TestCreateSession` |
```

**Step 2: Commit**

```bash
git add README.md
git commit -m "chore: rename agent-chat to GoHome in README"
```

---

### Task 4: Create Dockerfile

**Files:**
- Create: `Dockerfile`

**Step 1: Write the Dockerfile**

GoReleaser builds the binary outside Docker and copies it in. The Dockerfile only needs a minimal runtime image. Create `Dockerfile` at the project root:

```dockerfile
FROM alpine:3.21
RUN addgroup -S gohome && adduser -S gohome -G gohome
COPY gohome /usr/local/bin/gohome
USER gohome
EXPOSE 3000
ENTRYPOINT ["gohome"]
```

Note: No build stage is needed here because GoReleaser pre-builds the binary and passes it to Docker via its `extra_files` or direct COPY mechanism. The binary named `gohome` will be present in the Docker build context when GoReleaser runs this Dockerfile.

**Step 2: Commit**

```bash
git add Dockerfile
git commit -m "chore: add Dockerfile for GoReleaser Docker image builds"
```

---

### Task 5: Create CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`

**Step 1: Create the directory and file**

```bash
mkdir -p .github/workflows
```

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Test
        run: go test ./...

      - name: Vet
        run: go vet ./...

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Staticcheck
        run: staticcheck ./...
```

**Step 2: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add CI workflow with test, vet, and staticcheck"
```

---

### Task 6: Create GoReleaser config

**Files:**
- Create: `.goreleaser.yaml`

**Step 1: Write the config**

Create `.goreleaser.yaml` at the project root:

```yaml
version: 2

project_name: gohome

before:
  hooks:
    - go mod tidy

builds:
  - id: gohome
    main: ./cmd/agent
    binary: gohome
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: gohome
    builds: [gohome]
    name_template: "gohome_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "SHA256SUMS"
  algorithm: sha256

changelog:
  sort: asc
  use: github
  groups:
    - title: "New Features"
      regexp: '^feat'
      order: 0
    - title: "Bug Fixes"
      regexp: '^fix'
      order: 1
    - title: "Other Changes"
      order: 999
  filters:
    exclude:
      - '^chore'
      - '^docs'

release:
  draft: true
  replace_existing_draft: true

dockers:
  - image_templates:
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-amd64"
    use: buildx
    build_flag_templates:
      - "--platform=linux/amd64"
    goos: linux
    goarch: amd64
    dockerfile: Dockerfile

  - image_templates:
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-arm64"
    use: buildx
    build_flag_templates:
      - "--platform=linux/arm64"
    goos: linux
    goarch: arm64
    dockerfile: Dockerfile

docker_manifests:
  - name_template: "ghcr.io/jhyoong/gohome:{{ .Tag }}"
    image_templates:
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-amd64"
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-arm64"
  - name_template: "ghcr.io/jhyoong/gohome:latest"
    image_templates:
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-amd64"
      - "ghcr.io/jhyoong/gohome:{{ .Tag }}-arm64"
```

**Step 2: Validate the config locally (requires goreleaser to be installed)**

If GoReleaser is installed locally:
```bash
goreleaser check
```
Expected: `config is valid`

If not installed, skip — the release workflow will catch errors.

**Step 3: Commit**

```bash
git add .goreleaser.yaml
git commit -m "ci: add GoReleaser config for multi-platform binaries and Docker images"
```

---

### Task 7: Create release workflow

**Files:**
- Create: `.github/workflows/release.yml`

**Step 1: Write the workflow**

Create `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          cache: true

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**Step 2: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: add release workflow triggered by v* tags"
```

---

### Task 8: Final verification

**Step 1: Confirm no remaining agent-chat references in source files**

```bash
grep -r "agent-chat" --include="*.go" --include="*.md" --include="*.yaml" --include="*.yml" --include="Makefile" .
```
Expected: no output (zero matches). If any appear, fix them before continuing.

**Step 2: Run full test suite one more time**

```bash
go test ./...
```
Expected: all tests pass.

**Step 3: Build locally to verify binary name**

```bash
make build && ./gohome --version
```
Expected: prints `gohome dev`

**Step 4: Clean up**

```bash
make clean
```

**Step 5: Push to GitHub**

```bash
git push origin main
```

Once pushed, the CI workflow will run automatically on GitHub. Verify it passes at `https://github.com/jhyoong/GoHome/actions`.

---

### Task 9: Tag and trigger release

**Step 1: Create and push the v0.1.0 tag**

```bash
git tag v0.1.0
git push origin v0.1.0
```

**Step 2: Monitor the release workflow**

Go to `https://github.com/jhyoong/GoHome/actions` and watch the Release workflow run.

Expected outcome:
- 6 platform binaries built (linux/darwin/windows × amd64/arm64)
- Archives named `gohome_linux_amd64.tar.gz`, etc.
- `SHA256SUMS` file
- Docker manifest pushed to `ghcr.io/jhyoong/gohome:v0.1.0` and `:latest`
- Draft GitHub release created at `https://github.com/jhyoong/GoHome/releases`

**Step 3: Review and publish the draft release**

Go to `https://github.com/jhyoong/GoHome/releases`, open the draft, edit the changelog if needed, then click **Publish release**.
