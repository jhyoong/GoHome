version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
  disable:
    - funlen
    - gochecknoinits

formatters:
  enable:
    - gofmt
    - goimports
